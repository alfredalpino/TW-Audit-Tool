services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tw_audit
    volumes:
      - tw_audit_pg:/var/lib/postgresql/data

volumes:
  tw_audit_pg:
